// Physics resolution (Lower = faster, blurrier fluid; Higher = sharper, slower)
export const PHYSICS_WIDTH = 160;
export const PHYSICS_HEIGHT = 120;

// Gradient Control
export const GAMMA_CORRECTION = 2.2; // Higher = more contrast in darks
export const HEAT_DECAY = 0.92; // How fast heat dissipates (0-1)
export const HEAT_DIFFUSION = 0.6; // Blur strength per frame

// Particle System
export const MAX_PARTICLES = 150;
export const PARTICLE_SPAWN_RATE = 2;
